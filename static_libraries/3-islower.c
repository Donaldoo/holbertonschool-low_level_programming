#include "main.h"

/**
 * _islower - Entry point
 * @c: The character to print
 * Return: 1 if lowercase otherwise Return 0
 */

int _islower(int c)
{
	if (c > 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
